#ifndef ARITHMETIC_H_
#define ARITHMETIC_H_
#include "main.h"
void cplxDiv(cplx*, cplx*);
void cplxMul(cplx*, cplx*);
int cordic(int);
float absolute(float);
float square(float);
float getSigma(float*, int);
float getMean(float*, int);
#endif /* ARITHMETIC_H_ */
